use std::{collections::VecDeque, ops::Sub};

use crate::services::sorter::SortType;

use super::insertion_sort::insertion_sort;

pub fn bucket_sort(items: &mut Vec<u32>, steps: &mut Vec<VecDeque<SortType<u32>>>) {
    todo!()
}

